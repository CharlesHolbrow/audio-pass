#include <stdlib.h>
#include <string.h> // memset
#include "CCRing.h"
#include <stdio.h>


CCTap* createTap(CCRing* ring) {

  CCTap* tap = (CCTap*) malloc(sizeof(CCTap));
  if (tap == NULL) {
    return NULL;
  }

}
